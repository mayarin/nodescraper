extends layout

block content
  h1= title
  p Welcome to #{title}

  ul
    each data in list_data
      li: a(href="#{data.link}", target="_blank") #{data.title}
      br
      | #{data.body}
      br
